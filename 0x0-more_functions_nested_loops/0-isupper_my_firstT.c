#include "main.h"
#include <stdio.h>
#include <ctype.h>

/* Checks for uppercase character
 * Return: 0
 */
int main()
{
	char c;

	if (c = 'a';c <= 'z';c++)
	{
		if (c = 'c')
		{
		_putchar(c.isupper());
		}
		_putchar('\n');
		return (0);
		if (c != 'c')
		{
			_putchar(c.isupper());
			_putchar('\n');
			return (0);
		}
		else
		{
			write(1, "Entered value is not a character\n",33); 
		}
	}
}
