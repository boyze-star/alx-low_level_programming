#ifndef MAIN_H
#define MAIN_H
#include <stdio.h>
void positive_or_negative(int x);
int positive_or_negative(int t, int u, int v);
void print_remaining_days(int month, int day, int year);
int convert_day(int month, int day);
#endif
