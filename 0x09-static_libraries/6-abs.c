#include "main.h"

/**
 * _abs - computes absolute value of integer
 * @n: int to check
 *
 * Return: Absolute value of int.
 */
int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
