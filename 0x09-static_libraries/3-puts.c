#include "main.h"
#include <stdio.h>

/**
 * _puts - writes string to stdout
 * @str: str to be written
 *
 * Return: Void.
 */
void _puts(char *str)
{
}
/**
 * main - Entry point
 *
 * Return: Always 0(success), 1(Error)
 */
int main(void)
{
	puts("\"Coding practice makes better coder");
	return (0);
}
