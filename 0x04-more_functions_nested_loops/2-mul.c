#include "main.h"
/**
 * mul - checks for a digit multiplied two integers
 * @a: a Variable
 * @b: b Variable
 * Return: Always 0.
 */
int mul(int a, int b)
{
	int mul;

	mul = a * b;

	return (mul);
}
